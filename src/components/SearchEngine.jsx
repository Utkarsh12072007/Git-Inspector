import React from 'react';
import { 
  Search, MapPin, Link as LinkIcon, Calendar, Users, 
  Star, BookOpen, GitFork, Layout, Cpu, Twitter, Info 
} from 'lucide-react';

const Dashboard = ({ userData, repos }) => {
  return (
    <></>
  );
};

export default Dashboard;